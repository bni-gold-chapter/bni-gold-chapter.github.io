# BNI GOLD CHAPTER 新會員出村檢核追蹤表 — 專案完整紀錄

> 本文件記錄整個專案從無到有的完整過程、所有技術決策、踩過的坑與現況。
> 供導師協調員交接、以及 AI 助手（Claude / Codex）接手維護使用。
> 最後更新：2026/07

---

## 一、專案緣起

導師協調員原本用 Google 試算表（**新會員出村紀錄**）追蹤新會員的出村檢核進度，痛點：

- 表格欄位多、手機看不清楚
- 導師無法即時看到彼此更新
- 燈號、引薦、來賓、成交數字要另外去「紅綠燈檢視表」查，再手動抄

需求是做一個**可即時共用、手機可用、數據自動連動**的線上追蹤表。

### 兩個資料來源

| 來源 | 說明 |
|---|---|
| Google 試算表「新會員出村紀錄」 | 檢核項目與進度（左邊舊版 15 項、右邊新版 17 項） |
| [紅綠燈檢視表](https://service-2026-937515995986.us-west1.run.app/) | 全分會 43 人的燈號與各項尚缺指標（分會既有系統，賽車儀表風格） |

---

## 二、最終成果

### 網址

| 用途 | 網址 |
|---|---|
| **正式網址（發給導師）** | **https://bni-gold-chapter.github.io/** |
| 村民唯讀專屬連結 | `https://bni-gold-chapter.github.io/?view=<姓名>` |
| 資料來源 | https://service-2026-937515995986.us-west1.run.app/ |
| 原始碼 | https://github.com/bni-gold-chapter/bni-gold-chapter.github.io |

### 功能清單

- **舊版檢核（15 項）／新版檢核（17 項）／📦 已出村封存** 三個分頁
- **即時共用**：任一導師勾選或填備註，其他人畫面立即同步（Firebase）
- **響應式雙模式**：電腦整表總覽、手機自動切單人卡片，也可手動切換
- **燈號／引薦／來賓／成交自動連動**紅綠燈檢視表數據
- **📜 操作紀錄**：記錄每次狀態切換與備註修改的時間、操作者
- **🖼 匯出整表圖片**：舊版＋新版輸出成同一張 JPG
- **🔗 村民連結**：產生每位新會員的唯讀專屬網址
- **會員出村備註**可手動編輯，即時共用
- 賽事儀表（F1）視覺風格，與分會紅綠燈檢視表一致

---

## 三、技術架構

```
使用者瀏覽器
   │
   ├── index.html（單檔 HTML+CSS+JS，GitHub Pages 靜態託管）
   │
   └── Firebase Realtime Database（即時共用資料）
            ▲
            │ 手動執行 node update-refdata.js
            │
       紅綠燈檢視表（Cloud Run，無 CORS，只能由腳本抓）
```

**沒有後端伺服器**。整個網站是一個 `index.html`，資料放 Firebase。

### GitHub

- 組織：`bni-gold-chapter`
- Repo：`bni-gold-chapter.github.io`（此命名讓網址成為乾淨根網址）
- 推到 `main` → GitHub Pages 自動部署，約 1–3 分鐘生效

### Firebase Realtime Database

- 專案：`bni-tracker-b3ef8`
- 網址：`https://bni-tracker-b3ef8-default-rtdb.firebaseio.com`
- 規則：**永久開放讀寫**（原本測試模式 30 天到期，已於 2026/06/23 改永久）
  ```json
  { "rules": { ".read": true, ".write": true } }
  ```

前端設定（公開資訊，非機密）：
```js
const FIREBASE_CONFIG = {
  apiKey: "AIzaSyA9zxiowhwSmWF0hFgemvJiyeofEDvQs1o",
  authDomain: "bni-tracker-b3ef8.firebaseapp.com",
  databaseURL: "https://bni-tracker-b3ef8-default-rtdb.firebaseio.com",
  projectId: "bni-tracker-b3ef8",
  storageBucket: "bni-tracker-b3ef8.firebasestorage.app",
  messagingSenderId: "322793389801",
  appId: "1:322793389801:web:080a432df275e88783fa78",
  measurementId: "G-NLN6BRQYCQ"
};
```

### 資料節點

| 節點 | 內容 |
|---|---|
| `tracker_v7` | 進行中檢核資料。`{ old:[[狀態,備註],...], new:[...], oldNotes:[], newNotes:[] }`<br>狀態：`0`=未開始 `1`=完成 `2`=進行中 |
| `archive_v1` | 已出村封存。每筆含姓名、導師、燈號、完整檢核快照、封存時間 |
| `refdata` | 紅綠燈檢視表數據，依姓名對應 `{light, ref, o2o, guest, train, biz}` |
| `logs_v1` | 操作紀錄（時間／操作者／版本／會員／項目／動作） |

> ⚠️ `tracker_vN` 的版本號必須與 `index.html` 裡的 `KEY` 和 `dbRef` 一致。

---

## 四、檢核項目內容

### 舊版檢核（15 項）

| # | 項目 | 說明 | 負責 |
|---|---|---|---|
| 1 | Line 群組加入及使用說明 | 商務交流群 & 放入名片 | 副主席 |
| 2 | 個人名牌 & 新會員護照 | 直示當天提供 | 秘財 |
| 3 | 會員121資料提供 | 填入預計初版完成日期 | 導師 |
| 4 | 新會員護照第一次導讀 | 填入導讀日期 | 導師 |
| 5 | Zoom 及 Connect APP 安裝 | 熟悉工具 | 導師 |
| 6 | 確認了解綠燈計分方式 | 熟悉計分方式 | 導師 |
| 7 | 第二次導讀及與導師121 | 121技巧教學與演練 | 導師 |
| 8 | 與第二位導師121 | 121技巧教學與演練 | 導師 |
| 9 | 主席、副主席、成長「擇」121 | 加入6週內參加 | 擇二 |
| 10 | MSP教育訓練(導師陪同) | 加入6週內參加 | 導師 |
| 11 | 完成帶來一位來賓 | 加入6週內完成 | 導師 |
| 12 | 網頁版Connect 確認資料完整 | 加入6週內完成 | 導師 |
| 13 | 與導師協調員121 | 加入6週內完成 | 導師協調員 |
| 14 | 與董顧121 | 加入6週內完成 | 董顧 |
| 15 | 報名5分鐘專業簡報 | 結業後可主動報名 | 秘書財務 |

### 新版檢核（17 項）

| # | 項目 | 說明 | 負責 |
|---|---|---|---|
| 1 | Line 群組加入及使用說明 | 商務交流群 & 放入名片 | 副主席 |
| 2 | 個人名牌 & 新會員護照 | 直示當天提供 | 秘財 |
| 3 | 會員121資料填寫 | 填入預計初版完成日期 | 導師 |
| 4 | 新會員護照第一次導讀 | 填入導讀日期 | 導師 |
| 5 | Zoom 及 Connect APP 安裝 | 熟悉工具 | 導師 |
| 6 | 網頁版Connect 確認資料完整 | 熟悉工具 | 導師 |
| 7 | 確認了解綠燈計分方式 | 熟悉計分方式 | 導師 |
| 8 | 第二次導讀及與導師121 | 121技巧教學與演練 | 導師 |
| 9 | 與導師協調員121 | 121技巧教學與演練＋審核 | 導師協調員 |
| 10 | 與主席121 | 介紹分會發展＋南區 | 主席 |
| 11 | 與副主席121 | 解釋總政策與內規 | 副主席 |
| 12 | 與成長121 | 協助願景與規劃產業鏈 | 成長 |
| 13 | MSP教育訓練(導師陪同) | 加入6週內參加 | 導師 |
| 14 | 完成帶來一位來賓 | 加入6週內完成 | 導師 |
| 15 | 與支持董固121 | 加入6週內完成 | 支持董顧 |
| 16 | 與董顧121 | 加入6週內完成 | 董顧 |
| 17 | 綠燈結業後報名「專業短調」 | 擬定簡報定向 | 秘書財務 |

---

## 五、紅綠燈檢視表數據連動（重要）

### 資料在哪裡

紅綠燈檢視表**沒有 API**，資料是**直接打包在它的 JS 檔**裡：

```
https://service-2026-937515995986.us-west1.run.app/assets/index-<hash>.js
```

> `<hash>` 每次對方重新部署都會變，所以腳本要先抓首頁 HTML，用正則找出當前檔名。

資料格式：
```js
{id:43,name:"許柏祥",scores:{attendance:3,...,trafficLightScore:30,
  rolling6Months_referralDeficitWeekly:-5,
  rolling6Months_oneToOneDeficitBiweekly:-2,
  rolling6Months_businessValueDeficit:-8e5,
  rolling6Months_trainingDeficit:-6,
  rolling6Months_guestDeficit:-2}}
```

### 欄位對應

| 網站顯示 | JS 欄位 |
|---|---|
| 燈號 | `trafficLightScore` |
| 引薦尚缺 | `-rolling6Months_referralDeficitWeekly` |
| 一對一尚缺 | `-rolling6Months_oneToOneDeficitBiweekly` |
| 來賓尚缺 | `-rolling6Months_guestDeficit` |
| 培訓尚缺 | `-rolling6Months_trainingDeficit` |
| 成交尚缺 | `-rolling6Months_businessValueDeficit`（元，÷10000 顯示為萬） |

### 燈號顏色門檻

- 🟢 綠燈：≥ 70
- 🟡 黃燈：50–69
- 🔴 紅燈：< 50

### 為什麼要用腳本抓（不能瀏覽器直接抓）

紅綠燈檢視表**沒有開放 CORS**（`Access-Control-Allow-Origin` 是空的），瀏覽器直接 fetch 會被擋。
公開 CORS 代理（如 allorigins）實測回 522 不穩定。
**結論**：只能由腳本在本機／伺服器端抓取後寫進 Firebase，網頁再從 Firebase 讀。

### 更新方式（手動）

```bash
node update-refdata.js
```

執行後所有開著網頁的導師**即時看到新數據，不需重新整理**。
（曾評估設定「定時雲端工作」自動同步，使用者選擇**手動更新**即可。）

---

## 六、開發歷程

| 階段 | 內容 |
|---|---|
| 1 | 讀取試算表截圖 + 抓取紅綠燈檢視表，建立單檔 HTML 追蹤表 |
| 2 | 部署 GitHub Pages，取得固定網址 |
| 3 | 建立 Firebase 專案、Realtime Database，接上即時共用（用瀏覽器自動化完成全部設定） |
| 4 | 加入響應式雙模式（電腦整表 / 手機單人卡片） |
| 5 | 視覺改版：對齊紅綠燈檢視表的賽事儀表（F1）風格 |
| 6 | 修正 iOS 打勾顯示成灰色 emoji 的問題 |
| 7 | 新增操作紀錄頁、匯出整表 JPG |
| 8 | 會員出村備註改為可手動編輯 |
| 9 | 燈號／引薦／來賓／成交改為連動紅綠燈檢視表 |
| 10 | 新增「已出村封存」分頁 |
| 11 | 移到 GitHub 組織 `bni-gold-chapter`，取得乾淨根網址 |
| 12 | 表格比例統一、匯出雙表同圖、村民唯讀預覽 |

### 會員異動紀錄

| 動作 | 對象 | 處理方式 |
|---|---|---|
| 刪除 | 郭景翔、林士安 | 建置期尚無真實資料，直接重置版本 |
| 新增 | 吳至軒（法式甜點／導師黃奕樺） | 加入新版名單 |
| 刪除 | 邱士傑 | **資料搬遷**（已有真實資料，v5→v6） |
| 封存 | 邱士傑、洪倧勝 | 移入 `archive_v1`，從名單移除（v6→v7） |

### 資料版本演進

| 版本 | 原因 |
|---|---|
| v1–v2 | 初期建置 |
| v3 | 刪除郭景翔、林士安 |
| v4 | 新增吳至軒 |
| v5 | 資料結構加入「會員出村備註」陣列 |
| v6 | 刪除邱士傑（**首次採用資料搬遷，保留真實紀錄**） |
| v7 | 封存邱士傑、洪倧勝 |

---

## 七、⚠️ 最重要的鐵則

### 增刪會員 = 資料搬遷，絕不可重置

導師們已累積大量真實勾選與備註。**直接 bump 版本重來會全部清空。**

正確流程：
1. 讀取目前 `tracker_vN` 節點（先備份）
2. 只增／刪該會員那一欄（`old`/`new` 每一列 + `oldNotes`/`newNotes`）
3. 寫入 `tracker_v(N+1)`
4. 同步修改 `index.html`：`OLD_MEMBERS`／`NEW_MEMBERS`、`dfltOld`／`dfltNew` 的索引、`KEY`、`dbRef`
5. 舊節點**保留當備份**，確認無誤後再刪

參考腳本：`migrate-remove-member.js`、`archive-graduates.js`

### 其他原則

- 改任何雲端資料前，先 GET 下來備份
- push 後要輪詢網址確認新程式碼字串出現（CDN 有延遲，最多等 3 分鐘）

---

## 八、踩過的坑（重要技術筆記）

### 1. iOS 打勾變灰色
`✔`（U+2714）在 iOS 會被當**彩色 emoji** 顯示，CSS `color` 套不上去。
**解法**：加變體選擇器 → `✔︎`（U+2714 + U+FE0E）強制文字呈現。
顏色用螢光綠 `#35f59a` + 雙層 `drop-shadow` 做泛光。

### 2. html2canvas 無聲卡死
對 `left:-99999px` 的離畫面元素截圖時，Promise **永遠不 resolve**，不報錯也不完成。
**解法**：暫存容器放在畫面內（`position:fixed` + 高 `z-index`），並加 `Promise.race` 逾時保護。

### 3. 下載檔案用 Blob 不用 data URL
`canvas.toDataURL()` 產生的大型 data URL 程式化下載可能被瀏覽器忽略。
**解法**：`canvas.toBlob()` + `URL.createObjectURL()`。

### 4. PowerShell 中文亂碼
- `.ps1` 檔若無 BOM，PowerShell 5.1 會用系統編碼讀 → 中文變亂碼、腳本解析失敗
  → **腳本內避免中文字面值**（Node 版無此問題，優先用 Node）
- `Invoke-WebRequest`／`DownloadString` 預設編碼會弄壞 UTF-8 中文
  → 用 `DownloadData` + `[System.Text.Encoding]::UTF8.GetString()`

### 5. PowerShell 函式名撞內建別名
自訂函式命名為 `gv` 會被 `Get-Variable` 別名蓋過（別名優先於函式）。
**解法**：改名，例如 `getn`。

### 6. 表格欄寬不一
預設表格會依內容自動調整欄寬，導致每人欄位寬度不同。
**解法**：`table-layout:fixed` + 第一欄固定寬度 + `.mcard{min-height}` + `tbody tr{height}`。

### 7. GitHub 帳號要注意
本機曾同時登入兩個 GitHub 帳號，`gh` 建立的 repo 會跑到「作用中」帳號底下。
**檢查指令**：`gh auth status`、`gh api user --jq .login`
**切換**：`gh auth switch --user <帳號>` + `gh auth setup-git`

---

## 九、常用維護指令

```bash
# 更新紅綠燈數據（最常用）
node update-refdata.js

# 移除會員（保留其他人資料）
node migrate-remove-member.js tracker_v7 tracker_v8 <索引>

# 封存已出村會員（需依現況改寫腳本內容）
node archive-graduates.js

# 部署
git add -A && git commit -m "說明" && git push
```

---

## 十、目前狀態（2026/07）

### 進行中名單

**舊版檢核（15 項）**
| 會員 | 導師 | 行業 |
|---|---|---|
| 陳雨震 | 林淙仁 | 防水工程 |
| 夏振異 | 林淙仁 | 拆除工程 |
| 蔡家銘 | 李哲維 | 輕隔間 |

**新版檢核（17 項）**
| 會員 | 導師 | 行業 |
|---|---|---|
| 楊有成 | 林永恩 | 商業影像規劃 |
| 許柏祥 | 陳廷澐 | 系統櫃 |
| 余政學 | 張晉銘 | 居家設計 |
| 吳至軒 | 黃奕樺 | 法式甜點 |

### 已出村封存
| 會員 | 完成度 | 備註 |
|---|---|---|
| 邱士傑 | 15/15 | 7/2出村 |
| 洪倧勝 | 15/15 | 已出村 |

---

## 十一、交接說明

### 要移交的三樣東西

1. **GitHub 組織 `bni-gold-chapter` 的 Owner 權限**
   新任加入組織成為 Owner → 舊任退出。**網址與資料完全不變。**
2. **Firebase 專案 `bni-tracker-b3ef8` 的擁有者權限**（Google 帳號）
3. **本文件 + `CLAUDE.md` + `README.md`**（repo 內即有，看完即可接手）

### 新任如何維護

repo 內的 `CLAUDE.md` 是給 AI 看的指南。新任導師協調員只要：

1. 用手機或電腦開 Claude Code（或 Codex），連上此 repo
2. 直接用中文說需求，例如：
   - 「更新數據」
   - 「某某出村了，幫我封存」
   - 「新增會員 XXX，導師 OOO」
   - 「把打勾改成藍色」

AI 會自動讀 `CLAUDE.md` 了解架構與鐵則，執行後 push 即自動部署。

### 手機操作兩種方式

| 方式 | 說明 | 電腦需開機 |
|---|---|---|
| **GitHub repo** | 手機 Claude App → Code → 選此 repo，開新對話 | ❌ 不用 |
| **Remote Control** | 電腦上執行 `/remote-control`，手機掃 QR 接管同一個對話 | ✅ 必須 |

> Remote Control 需要 Pro/Max/Team/Enterprise 方案，且必須用 claude.ai 帳號登入（不支援 API key）。

---

## 十二、待處理事項

- [ ] **舊部署清理**：`a7709393` 帳號（非使用者本人帳號）底下仍有 `bni-tracker` 與 `BNI-GOLD-CHAPTER` 兩個 repo，其部署仍可讀寫同一個 Firebase。建議請該帳號持有者刪除，或改為導向新網址。
- [ ] `nicko751031-a11y/BNI-GOLD-CHAPTER` 已轉移至組織，個人帳號下應已無殘留。
- [ ] `archive-graduates.js` 目前是一次性寫死的腳本，未來封存需改寫或請 AI 重新產生。
- [ ] 村民唯讀連結為「知道網址即可看」，若需更嚴格權限需另建帳號系統。
